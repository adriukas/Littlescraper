services:
  app:
    build: .
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    depends_on:
      - db

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: discord_scraper
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"